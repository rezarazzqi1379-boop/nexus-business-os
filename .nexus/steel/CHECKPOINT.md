# CHECKPOINT — PRJ-STEEL-ROLLING-LINE-01

**آخرین به‌روزرسانی این بخش (بیلت، SUPERSEDED):** ۲۰۲۶-۰۹-۲۱ (پس از تأیید مالک و بستن C-07). آخرین چک‌پوینت فعال زندهٔ این فایل پایین صفحه است (۲۰۲۶-۰۹-۲۶).
**project_id:** `rolling_mill_strip_300_pilot`
**authority:** Master Context v1.4 + Source Registry v1.1 (commit `68c0832`) — **حل شد ۲۰۲۶-۰۹-۲۶:** `AUTHORITY_STATUS.md` (تاریخ واقعی ۲۰۲۶-۰۹-۱۷) RECONCILED PARTIAL می‌گوید؛ نسخهٔ ۲۰۲۶-۰۹-۱۹ فایل `CURRENT_STATE.md` که UNRESOLVED BLOCKING می‌گفت صرفاً هرگز به‌روز نشده بود (خودِ آپدیت authority دو روز زودتر، در ۰۹-۱۷ اتفاق افتاده بود). `CURRENT_STATE.md` در ۰۹-۲۶ بازنویسی و اصلاح شد.

## وضعیت گیت
- `calculation_allowed` (گیت قدیمی، دست‌نخورده): **False**
- `concept_calculation_allowed`: **True**
- `fabrication_release_allowed`: **False** — ۱۱ مانع

## آخرین کار
تعارض حاکمیتی C-07 بسته شد (تفکیک دو-گیتی تأیید و ثبت شد؛ متن agent بازنویسی شد؛ جایگزین skill پیشنهاد شد). دو پرسش‌نامه‌ی دور ۴ و ۵ که فقط در یک برنچ محلی push‌نشده بودند نجات داده شدند. سه پیش‌نویس خارجی آماده‌ی ارسال توسط مالک است.

پیش از آن: Kernel دائمی: preflight، مسیریابی، TTL، بودجه‌ی توکن، رویداد ورود شواهد، و هشت replay test بند ۱۴.

## وضعیت مهندسی — سطح تجربه‌ی واقعی
| قلم | سطح |
|---|---|
| مدل مفهومی `rolling_line_concept` | **L1** — و بازبین مستقل REJECT داد (پاس حاکم چک نشده، پلاک موتور آشتی نمی‌دهد، حساسیت دما نتیجه را می‌شکند) |
| الگوریتم κ `caliber_spread_model` | **L2** — فقط داده‌ی مصنوعی |
| بودجه‌ی خطای اندازه‌گیری | **L1، ردشده** — FM-001 |
| IC-02 نسخه ۰.۲ | **L0** |
| هر چیز فیزیکی | **هیچ** |

## داده‌ی STALE یا UNKNOWN
- قیمت واقعی تسمه/ورق عرض ۲۵۰–۳۰۰ mm: **در هیچ منبعی پیدا نشد**
- پریمیوم ST52: فقط Tier 5 و **قیمت پیشنهادی**، نه معامله‌شده
- جنس و سختی غلتک: **UNKNOWN** — تعیین‌کننده‌ی عریض‌سازی
- ظرفیت نیروی اسمی اصلی ST1: **UNKNOWN** — درس Ternium-Siderar
- دور موتور ST2: **UNKNOWN** — فرضیه ۷۰۰–۸۰۰ rpm، آزمون‌نشده
- دمای فرآیند: **هرگز اندازه‌گیری نشده**
- کوره: **هیچ داده‌ای**
- معنای جدول P1–P10: **UNKNOWN**

## next_action
کمپین اندازه‌گیری M1 — یک شیفت، ۲۰ شمش. قالب‌گیری شیار + دورریز سرد + کلمپ CT + پلاک ST2 + جنس غلتک + ظرفیت اسمی استند.

## تأیید لازم
- **ارسال** سه پیش‌نویس آماده (پرسش‌نامه دور ۵ به مهندس، پیش‌فاکتور ST52/ST37، استعلام آزمایشگاه) — **با دست شما، نه من**
- **خرید** کتاب سال فولاد ۱۴۰۵ و دسترسی آرشیو بورس کالا
- **ورود به سالن** برای کمپین M1

هیچ خرید، ارسال خارجی یا تغییر ماشینی انجام نشده است و از من برنمی‌آید.

## token_class آخرین کار
T2 — یک agent بازبین، بدون پژوهش زنده.

---

## چک‌پوینت ۲۰۲۶-۰۹-۲۲ · **خط اسلب — پروژه‌ی فعال**

> **هشدار وضعیت:** همه‌ی چک‌پوینت‌های بالای این خط مربوط به **خوراک شمش ۱۵۰×۱۵۰** است و از ۲۰۲۶-۰۹-۲۱ به دستور مالک **SUPERSEDED — DO NOT USE** شده‌اند. رجوع به `docs/expert_foundry/SUPERSEDED_NOTICE_2026-09-21.md`.

### مبنای فعال (FACT، اعلام مالک ۲۰۲۶-۰۹-۲۱)

اسلب **۴۰۰×۱۲۵×۳۰۰۰** (۱۱۷۷٫۵ kg) · محصول **۴۰۰ mm عرض، ۶ تا ۳۰ mm** · کوره **۲۰ t/h** · خروج اسلب **۱۲۵۰°C** · استند **دوغلتکه** Ø۶۰۰ / بشکه ۶۰۰ · سقف سرعت **۳ m/s** · درایو **DC**

### وضعیت مدل

| قلم | وضعیت |
|---|---|
| `slab_line_design.py` | IMPLEMENTED · TESTED (۷۰ تست) · COMMITTED · **نه MERGED، نه ACTIVE** |
| برنچ | `feat/steel-recovery-v0.1` @ `bfc4d97` — **روی remote نیست، PR باز نیست** |
| `origin/main` | `272d1996` — دست‌نخورده |
| کل مجموعه تست | ۱۵۸۰ سبز |

### تصمیم‌های فنی فعلی

| قلم | مقدار | وضعیت |
|---|---|---|
| پاکت فاز اول | ۴۰۰ × ۱۲–۳۰ mm | **[RDR]** |
| فاز توسعه | ۶–۱۰ mm | مشروط |
| نیروی طراحی | ۵٫۱۲ MN | محاسبه‌شده (S355JR) |
| گشتاور بار | ۲۱۸٫۳ kN·m | محاسبه‌شده |
| نسبت گیربکس | **Preferred Preliminary ۱:۷٫۱** · RFQ ۶٫۳–۸٫۰ | **[RDR]** |
| گیربکس نامی | ≥ ۳۳۰ kN·m · پیک ≥ ۶۵۵ | **REQUIREMENT** |
| موتور | DC ۱۶۰۰ یا ۲۰۰۰ kW، پایه ۳۵۰، حداکثر ۷۰۰ | **هر دو در استعلام، هیچ‌کدام انتخاب نشده** |
| سنتر pinion | **Preferred Preliminary ۶۴۶ mm** | مشروط به GA |

### Hold Pointهای باز

H-1 گردن/یاتاقان · H-2 فریم و پیچ تنظیم · H-3 برگشتی یا یک‌طرفه · H-4 طول سالن · H-5 گرید · H-6 درایو موجود · **H-7 مدول آسیاب** · **H-8 GD² و زمان شتاب** · **H-9 حداقل قطر کاری و طول اسپیندل**

### آنچه از بسته‌ی قبلی تصحیح شد

چهار خطای بازبینی بیرونی (`2c12584`) و سه خطای بازبینی مالک (`91469c3`) — جزئیات در `ENGINEERING_PACKAGE_A_TECHNICAL_2026-09-21.md` بخش ۸ و ۹، و FM-005 در دفترچه شکست.

### دفعه‌ی بعد از اینجا شروع کن

۱. بازبینی diff برنچ · ۲. تصمیم مسیر PR · ۳. اجرای Hold Pointهای دسته الف (۹ مورد، هزینه‌ی تقریباً صفر) · ۴. خرید متن DIN 59200

---

## چک‌پوینت ۲۰۲۶-۰۹-۲۶ · بررسی تازگی وضعیت (state freshness) — بدون تغییر مهندسی

کار این نشست روی خودِ ماشین تداوم بود، نه روی طراحی خط. خلاصه:

- **بررسی جدید:** `nexus_checks/state_freshness.py` — یک فایل وضعیت/kernel وقتی STALE علامت می‌خورد که مسیری که حاکم است (طبق `.nexus/state/STATE_GOVERNANCE.json`) کامیت تازه‌تر از خودِ فایل وضعیت داشته باشد (تحمل پیش‌فرض ۲ روز)؛ ارجاع‌های `` `مسیر` `` شکسته در همان فایل‌ها هم پیدا می‌شوند. اجرا: `python -m nexus_checks --state --repo .`. تست: `evals/test_nexus_checks_state.py`.
- **چه چیزی STALE بود (پیش از این commit):** `.nexus/state/CURRENT_STATE.md` حدود ۷ روز عقب بود (آخرین ویرایش ۲۰۲۶-۰۹-۱۹) در برابر تغییرات `docs/procurement/`، `docs/expert_foundry/`، `docs/system/`، `.nexus/steel/` و `AGENTS.md`. همین فایل CHECKPOINT هم حدود ۴ روز عقب بود. هر دو در این commit به‌روز شدند.
- **ارجاع شکسته پیدا و اصلاح شد:** `.nexus/steel/GAP_ANALYSIS.md` به `registers/*.md` اشاره می‌کرد، بدون پیشوند `.nexus/expert_foundry/`؛ اصلاح شد. `CURRENT_STATE.md` مسیر یک برنچ مرج‌نشده (src/nexus_control_plane/forge.py) را طوری نوشته بود که انگار مسیر واقعی ریپو است؛ بازنویسی شد تا روشن باشد فقط روی یک برنچ ادغام‌نشده وجود داشت. `PROJECT_CONTROL...md`، ردیف OPS-04، به .claude/helpers/auto-commit.sh را طوری ارجاع می‌داد که انگار tracked است؛ اصلاح شد (هرگز commit نشده بود).
- **یافتهٔ جانبی (نه از ابزار جدید، از خواندن مستقیم):** `CURRENT_STATE.md` نسخهٔ ۲۰۲۶-۰۹-۱۹ هنوز وضعیت authority را UNRESOLVED/BLOCKING می‌نوشت، در حالی که `docs/authority/AUTHORITY_STATUS.md` از ۲۰۲۶-۰۹-۱۷ (دو روز زودتر) RECONCILED (PARTIAL) شده بود. این فایل هرگز آپدیت را نگرفته بود؛ در بازنویسی امروز اصلاح شد.
- **بدون تغییر:** هیچ عدد مهندسی، Hold Point یا وضعیت گیت این نشست تغییر نکرد. Gate ها همان‌طور که بالای همین فایل آمده باقی‌اند.
- **بعدی:** پس از هر ویرایش در `docs/procurement/`، `docs/expert_foundry/`، `.nexus/steel/` یا `docs/system/`، همان نشست `python -m nexus_checks --state --repo .` را اجرا و `CURRENT_STATE.md`/این فایل را به‌روز کنید — نه بعداً.
