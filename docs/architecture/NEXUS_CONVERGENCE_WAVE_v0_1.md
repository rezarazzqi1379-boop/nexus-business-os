# NEXUS Convergence Wave v0.1

Status: REVIEWABLE INTERNAL CONSOLIDATION — NO PRODUCTION EFFECT.

## Canonical basis
- Master Context v1.9
- Source Registry v1.6
- current main `95e46c2e6d58aa771dc2ade8fa8381f77e9776e6`

## Goal
Reduce architecture sprawl while increasing autonomous completion. Do not merge the historical open stack as a group. For each behavior, identify its canonical owner, compare it with current main, transplant only missing tested behavior, and retire duplicate runtime/authority paths.

## Target operating architecture
`Executive Kernel -> Canonical Authority -> Project Isolation -> Evidence/Contradiction Gates -> Resource Router/Provider Economy -> Governed Worker -> Deterministic Evals/Arena -> Durable Shadow Execution -> Audit/Outcome Learning -> Exact Consequential Action Gate`

Workers, models, MCPs, plugins and automations are adapters. None can become business, engineering, approval or source authority.

## Fast multi-step sequence
1. **Authority already closed:** Canonical Authority v2 is merged/tested on main.
2. **Worker lane:** keep PR #64 as the smallest OpenCode sandbox adapter; no merge until a sanitized live pilot pins exact runtime/model/provider and current retention terms.
3. **Measurement lane:** keep PR #65 stacked on #64; first pilot uses identical task, repository snapshot and acceptance contract. Unsafe/failing candidates lose regardless of speed/cost.
4. **Runtime convergence:** treat PR #40 as the durable-execution candidate; compare #54 and #17 against it and extract only missing queue/scheduler/recovery behavior.
5. **Security convergence:** extract exact-action approval from #37, prompt-injection ingress regression from #15, and audit metadata semantics from #10 onto current main only if missing.
6. **Learning convergence:** extract immutable decision/outcome lineage from #7, bounded evolution evaluation from #19, and stage-aware outcome/negative knowledge from #33 without adding a second promotion authority.
7. **Research/productization:** retain #14/#39 only as measured experiments. No promotion without observed outcome improvement.
8. **Retire historical duplication:** #9 is integration-only; #24 is partially superseded; #30 is stale as a canonical checkpoint; #35 is incubator research. Close only after required evidence/behavior is preserved.

## Current external research refresh — 27 Aug 2026
OpenCode v2 documentation currently labels the release beta and warns that APIs/config/plugin APIs may change. The current install binary is `opencode2`; model selection should use the live provider/model catalog rather than guessed model IDs. OpenCode supports provider/model references and custom OpenAI-compatible providers. This confirms the NEXUS decision to keep OpenCode experimental and version-pinned at pilot time rather than hard-code `unverified-latest` as production truth.

Z.ai's 26 Aug release identifies `ox-alpha` as GLM-5.3-Flash. Vendor-published benchmark numbers remain CLAIM/MEASUREMENT-BY-VENDOR until reproduced on the NEXUS Arena.

## Acceptance for this convergence wave
- no duplicate canonical owner for graph/runtime/approval/eval/audit/evolution;
- current main remains deploy-independent and production claims remain false until verified;
- all extracted behavior receives current-main regression tests;
- no stale project specification is copied from old PR fixtures;
- at least one sanitized coding vertical produces real Arena observations before OpenCode adapter promotion;
- at least one read-only real-project vertical reaches durable queue -> recovery/audit -> measured outcome before production runtime promotion.

## Stop conditions
Stop and request exact approval before merge of a new convergence PR, deployment, production DB/permission change, external message/publication, payment, contract, purchase, signature or destructive action.
