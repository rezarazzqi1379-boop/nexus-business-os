[project]
name = "nexus-business-os"
version = "0.1.0"
description = "NEXUS Business OS coded verticals"
requires-python = ">=3.11"

[project.optional-dependencies]
test = [
  "pytest>=8",
  "hypothesis>=6.100",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
